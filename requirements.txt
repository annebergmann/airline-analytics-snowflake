apache-airflow==2.10.4              #pinned to a specific version. Always pin Airflow, it releases frequently and breaking changes are common.
requests==2.32.3                    # the HTTP library our OpenSky client uses to call the API.
snowflake-connector-python==3.12.3  #the official Snowflake driver for inserting rows into RAW_FLIGHTS.